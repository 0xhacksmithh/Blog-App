export const sendEmail = (contacts) => {
  for (let i = 0; i < contacts.length; i++) {
    console.log(`Sending Email To ${contacts[i]}.....`);
  }
};

export const sendSMS = (contacts) => {
  for (let i = 0; i < contacts.length; i++) {
    console.log(`Sending Email To ${contacts[i]}.....`);
  }
};

export const sendWhatsApp = (contacts) => {
  for (let i = 0; i < contacts.length; i++) {
    console.log(`Sending Email To ${contacts[i]}.....`);
  }
};
